// version
import { PROJECT_VERSION } from '@/config.ts';

export default {
    name: 'footer',
    components: {},
    props: [],
    setup() {
        return {
            PROJECT_VERSION
        };
    }
};
